<nav class="navbar navbar-default">
  <div class="container-fluid">
    {{navbar-header}}

    <div class="collapse navbar-collapse" id="navigation">
      <ul class="nav navbar-nav">
        {{#if isAuthenticated}}
          <li>
            {{#link-to "users"}}Users{{/link-to}}
          </li>
        {{/if}}
      </ul>
      <ul class="nav navbar-nav navbar-right">
        {{#if isAuthenticated}}
          <li>
            {{#link-to "change-password"}}Change Password{{/link-to}}
          </li>
          <li><a href="#" {{action "signOut"}}>Sign Out</a></li>
        {{else}}
          <li>
            {{#link-to "sign-up"}}Sign Up{{/link-to}}
          </li>
          <li>
            {{#link-to "sign-in"}}Sign In{{/link-to}}
          </li>
        {{/if}}
      </ul>
    </div>
  </div>
</nav>

<h1>Rate my Photos</h1>

<h3>For Photographer's By Photographer's</h3>

{{#each flashMessages.queue as |flash|}}
  {{flash-message flash=flash}}
{{/each}}

<div class="col-md-8 col-md-offset-2">
  {{outlet}}
</div>


<h6>Greetings</h6>
<div class="greeting">
<p>This site is desined for Photographers who want to show their work to others and/or help photographers get better! Sign up and then rate photos. Post your own photos and see the ratings you receive!
</p>
</div>

{{#if isAuthenticated}}
  {{#link-to "your-photos"}}Your Photos{{/link-to}}
{{/if}}


{{#if isAuthenticated}}
  {{#link-to "view-photos"}}Rate these Photos{{/link-to}}
{{/if}}
